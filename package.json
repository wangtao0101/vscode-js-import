{
    "name": "vscode-js-import",
    "displayName": "vscode-js-import",
    "description": "",
    "version": "0.0.1",
    "publisher": "yuecjnadt",
    "engines": {
        "vscode": "^1.12.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.sayHello"
    ],
    "main": "./out/src/extension",
    "contributes": {
        "commands": [{
            "command": "extension.sayHello",
            "title": "Hello World"
        }],
        "configuration": {
            "type": "object",
            "title": "js import configuration",
            "properties": {
                "js-import.root": {
                    "type": "string",
                    "default": "/src",
                    "description": "root to resolve absolute path and relative path"
                },
                "js-import.alias": {
					"type": "object",
					"description": "module alias",
					"default": ""
				}
            }
        }
    },
    "scripts": {
        "vscode:prepublish": "tsc -p ./",
        "compile": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.0.3",
        "vscode": "^1.0.0",
        "mocha": "^2.3.3",
        "@types/node": "^6.0.40",
        "@types/mocha": "^2.2.32"
    }
}